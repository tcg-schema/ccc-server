# Core runtime deps — used by Vercel (`pip install -r requirements.txt`).
# WeasyPrint is intentionally NOT here: Vercel's runtime lacks the Pango/Cairo
# system libraries, so /render/pdf degrades to HTTP 501 there. For a
# render-capable deployment use the Docker image (setup.cfg installs weasyprint)
# or `pip install weasyprint` locally alongside the system libs.
fastapi==0.120.1
starlette==0.49.1
uvicorn==0.13.4
httpx==0.28.1
pydantic>=2
python-dotenv==0.17.1
